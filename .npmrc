# Electron main runs from out/build/electron/ and must resolve native/runtime
# deps from the repo root. Hoist desktop-only packages used by the main bundle.
public-hoist-pattern[]=better-sqlite3
public-hoist-pattern[]=jsdom
public-hoist-pattern[]=node-llama-cpp
public-hoist-pattern[]=@mozilla/readability
