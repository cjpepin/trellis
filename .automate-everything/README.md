# Automate Everything — Infrastructure Config

This directory was generated by [Automate Everything](https://github.com/automateeverythingapp/automate-everything) and describes the hosted infrastructure for this repository.

- **Platform project:** `1d51271b-dd01-4499-9a2e-9f25791d997c`
- **Schema:** `ae.iac.v1`
- **Providers:** Vercel, Supabase

## Local OpenTofu setup

```bash
cd .automate-everything/infrastructure
tofu init
```

Copy `.automate-everything/env.example` to a local `.env` or export variables before apply:

```bash
cp ../env.example ../.env.local
# Fill in secret values — they are NOT stored in Git
export TF_VAR_vercel_api_token="..."
export TF_VAR_supabase_access_token="..."
tofu plan
```

## Secrets

Provider API tokens and database passwords are **never** committed to this repository. Copy them from the Automate Everything deployment Complete page when prompted.

## Files

| File | Purpose |
|------|---------|
| `manifest.json` | Machine-readable config for redeploy and drift detection |
| `env.example` | Environment variable names for local tooling |
| `infrastructure/*.tf` | OpenTofu resources managed by Automate Everything |

